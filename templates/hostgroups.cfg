{% for host_group in groups %}
{% if host_group not in ("ungrouped", "all") and groups[host_group]|length > 0 %}
define hostgroup {
  hostgroup_name {{ host_group }}
  members {{ groups[host_group]|join(',') }}
}
{% endif %}
{% endfor %}
